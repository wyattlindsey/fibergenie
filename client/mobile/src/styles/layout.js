import { StyleSheet } from 'react-native'

const fullHeight = {
  height: '100%',
}

const fillWidth = {
  width: '100%',
}

const styles = StyleSheet.create({
  fullHeight,
  fillWidth,
})

export default styles
